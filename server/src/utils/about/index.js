const { formatService } = require("./serviceFormatters");
const { formatTrigger } = require("./triggerFormatters");
const { formatAction } = require("./actionFormatters");

module.exports = {
    formatService,
    formatTrigger,
    formatAction,
};
